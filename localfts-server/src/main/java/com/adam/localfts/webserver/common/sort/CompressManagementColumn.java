package com.adam.localfts.webserver.common.sort;

public enum CompressManagementColumn {
    FOLDER_NAME,COMPRESS_STATUS,COMPRESS_FILE_SIZE,COMPRESS_FILE_LAST_MODIFIED,COMPRESS_START_TIME,COMPRESS_FINISH_TIME,COMPRESS_COST_TIME
}
